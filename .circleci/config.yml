version: 2.1

parameters:
  nombre:
    type: string
    default: elPocho

jobs:
  saluda:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo Hola << pipeline.parameters.nombre >>

workflows:
  workflow1:
    jobs:
      - saluda
