# Couldn't automatically generate a config from your source code.
# This is a generic template to serve as a base for your custom config
# See: https://circleci.com/docs/configuration-reference
version: 2.1

jobs:
  build:
    docker:
      - image: circleci/sha256:01bd2a33ca24c7884677372db42ac8309c174f23c7ed30b3fe0aba5256b836e4
        auth:
          username: 67951926
          password: $MY_DOCKERHUB_PASS
    steps:
      - checkout

      - run:
          name: Construir imagen Docker
          command: docker build -t getnasapi .

      - run:
          name: Ejecutar imagen Docker
          command: docker run --name getnasapi getnasapi  $MY_APIKEY_VAR

      - run:
          name: Imprimir resultado en formato JSON
          command: docker cp getnasapi:/app/nasaResults/neowsResponse.json ./output/
            cat ./output/neowsResponse.json
